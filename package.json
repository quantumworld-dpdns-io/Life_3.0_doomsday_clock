{
  "name": "life-3-doomsday-clock",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix apps/web run build",
    "dev": "npm --prefix apps/web run dev",
    "install:web": "npm --prefix apps/web install",
    "lint": "npm --prefix apps/web run lint",
    "test": "npm --prefix apps/web test -- --run"
  },
  "engines": {
    "node": ">=20"
  }
}
